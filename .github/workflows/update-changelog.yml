name: Deploy to Neocities

on:
  push:
    branches:
      - main
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Deployment handled by local pre-push hook
        run: echo "Deployment is handled by the local pre-push hook. No action needed here."
